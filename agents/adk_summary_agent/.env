# Required: Secret Manager details for Google API Key
SECRET_PROJECT_ID=your_gcp_project_id_for_secrets
GOOGLE_API_KEY_SECRET_ID=your_secret_id_for_google_api_key

# Optional: Set to TRUE to use Vertex AI instead of API Key fetched from Secret Manager
GOOGLE_GENAI_USE_VERTEXAI=0
# GOOGLE_CLOUD_PROJECT=your-gcp-project-id # Required if using Vertex
# GOOGLE_CLOUD_LOCATION=your-gcp-region   # Required if using Vertex

# Required: URLs or commands for the MCP servers hosting specific Summarization tools
# Ensure these URLs/commands point to *separate* servers if necessary.
MCP_URL_SUMMARIZE=your_mcp_server_url_or_command_for_summarize_video
MCP_URL_COMBINE=your_mcp_server_url_or_command_for_combine_summaries

# Required: URL for the LangGraph YouTube Agent A2A endpoint
YOUTUBE_AGENT_A2A_URL=http://localhost:10003 # Default, change if needed

# Optional: Path to service account JSON if MCP servers require auth
# SERVICE_ACCOUNT_JSON_SUMMARY=/path/to/your/summary-sa.json
